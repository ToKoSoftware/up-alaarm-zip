export enum AlaarmErrors {
    NOT_FOUND = <any>'The requested resource could not be found',
    SCENARIO_ALREADY_RUNNING = <any>'The scenario is already running',
    SCENARIO_NOT_RUNNING = <any>'The scenario is not running yet',
    // eslint-disable-next-line
}
